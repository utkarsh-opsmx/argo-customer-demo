apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: single-helm-source1
  namespace: argocd
  labels:
    for: jpmc
spec:
  destination:
    namespace: argocd-kt
    server: 'https://kubernetes.default.svc'
  source:
    repoURL: 'https://github.com/OpsMx/argo-customer-demo.git'
    path: source-data/helm/single-helm
    targetRevision: testcase
    helm:
      valueFiles:
        - values.yaml
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
