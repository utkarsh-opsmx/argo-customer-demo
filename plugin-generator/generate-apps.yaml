apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-plugin-script
  namespace: argocd
data:
  generate-apps.sh: |
    #!/bin/bash
    cat <<EOF
    [
      {
        "name": "app1",
        "namespace": "argocd-kt"
      },
      {
        "name": "app2",
        "namespace": "test"
      }
    ]
    EOF
